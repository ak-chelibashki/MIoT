# Config
[defaults]
inventory = inventory
roles_path = ./roles
remote_user = chelibat
host_key_checking = False
retry_files_enabled = False
interpreter_python = auto_silent
private_key_file = ~/.ssh/id_ed25519

[privilege_escalation]
become = True
become_method = sudo
